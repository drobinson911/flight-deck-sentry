# Copy to secrets.properties (gitignored). Values are baked into BuildConfig as
# DEFAULTS only; the pilot can override every one in Settings on the device.
#
# Fleet read credential for /api/live/our-drones and /api/live/dronesense
# (X-Fleet-Token header). On the maintainer's build machine: a local token file
FLEET_TOKEN=
