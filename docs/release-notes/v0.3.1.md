## Sentry 0.3.1

- Update panel: the status line now reflects the installed version after an update ("up to date" instead of a stale "0.3.0 is available").
- First release intended for a real in-place update test (0.3.0 → 0.3.1 via Settings → Check for update).
